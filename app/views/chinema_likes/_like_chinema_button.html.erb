<% if user_signed_in? %>
    <% if chinema.chinema_liked_by?(current_user) %>
        <p><span>お気に入り解除: </span><%=link_to chinema.chinema_likes.count, chinema_chinema_likes_path(chinema.id), method: :delete, remote: true %></p>
    <% else %>
        <p><span>お気に入り登録: </span><%=link_to chinema.chinema_likes.count, chinema_chinema_likes_path(blog.id), method: :post, remote: true %></p>
    <% end %>
<% else %>
    <p><span>お気に入り数: </span><%= chinema.chinema_likes.count %></p>
<% end %>